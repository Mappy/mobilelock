var should = require('should');
var _ = require('underscore');

describe('Mobilelock client', function() {

    describe('mobilelock.js#?', function() {

        it('should pass', function() {
            // Given
            var i = 1;

            // When
            
            // Then
            i.should.eql(1);
        });

    });
});
